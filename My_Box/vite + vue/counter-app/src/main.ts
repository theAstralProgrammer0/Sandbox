import { createApp } from 'vue'; // Import the createApp function from Vue
import './style.css'; // Import the global styles
import App from './App.vue'; // import the App component

createApp(App).mount('#app'); // Create a Vue app instance and mount it to the #app div
